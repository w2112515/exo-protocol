# Exo Protocol 前端设计方案 v2.0

**文档类型**: CSA 架构决策文档
**创建日期**: 2024-12-15
**状态**: ✅ CSA APPROVED (ADR-011)
**修订**: v2.0 (Terminal Minimalism - Living Data Fusion)

---

## 1. Phase 3 进入确认

### 前置条件检查

| 条件 | 状态 | 说明 |
|------|------|------|
| Phase 1 协议层 | 🟡 PARTIAL | 合约构建成功，IDL 部分阻塞 (可并行) |
| Phase 2 SRE 运行时 | ✅ 核心完成 | SANDBOX✅ COMMITTER✅ ORCHESTRATOR✅ |
| P3-MOCK 数据 | ✅ AUDITED | 15 Orders + 8 Skills 可用 |
| Node.js 环境 | ✅ v22.12.0 | 前端开发环境就绪 |

**结论**: ✅ 可进入 Phase 3 前端开发

---

## 2. 设计目标

### 2.1 核心叙事 (评委视角)

> **"让评委在 30 秒内感知 Exo Protocol 的精密与优雅"**

- 🎯 **极简呈现**: 剔除视觉噪音，聚焦交易流动的本质 (Order → Execution)
- 🎯 **质感交互**: 通过微动效、模糊与光影传达协议的"厚重感"与"可信度"
- 🎯 **清晰透明**: 资金流向一目了然 (Executor 85% / Creator 10% / Protocol 5%)

### 2.2 用户角色

| 角色 | 核心诉求 | 主要功能 |
|------|---------|---------|
| **评委/访客** | 快速直观地理解机制 | 沉浸式 Hero + 极简实时流 + 交互式数据 |
| **Skill 创作者** | 优雅地管理资产 | 资产卡片 + 收益透视 |
| **Agent 执行者** | 高效监控任务 | Agent 仪表盘 + 状态流 |

---

## 3. 技术栈规范 (Stack) - v2.0 升级

### 3.1 核心依赖

```json
{
  "framework": "Next.js 15 (App Router + Server Actions)",
  "styling": "TailwindCSS + shadcn/ui + CVA",
  "style_engine": "CSS Variables + Backdrop Filter",
  "state": "TanStack Query 5.x + Zustand 4.x",
  "visualization": {
    "charts": "Recharts 2.x (Customized)",
    "flow": "React Flow (Node Graph)",
    "3d": "@react-three/fiber + @react-three/drei (Hero Only)"
  },
  "wallet": "@solana/wallet-adapter-react",
  "icons": "Lucide React (Stroke 1.5px)",
  "animation": "Framer Motion (Spring Physics)",
  "fonts": "Inter Tight (UI) + JetBrains Mono (Terminal/Data)"
}
```

### 3.2 技术栈升级理由 (ADR-011)

| 升级项 | 理由 |
|-------|------|
| **Next.js 15** | Server Actions 更丝滑，稳定版已发布 |
| **TanStack Query** | Web3 链上数据轮询/缓存刚需 |
| **React Flow** | Agent 节点关系图，比静态 Sankey 更直观 |
| **R3F (Hero Only)** | 轻量粒子网络，展示"活体数据"概念 |
| **CVA** | shadcn/ui 底层，管理组件变体状态 |

### 3.3 风格约束 (Constraints)

- ❌ **拒绝**: 粗糙的霓虹发光 (High Contrast Neon)
- ❌ **拒绝**: 纯黑背景 (Pure Black) -> 使用 **Zinc-950/Noise**
- ❌ **拒绝**: 默认的卡片阴影 -> 使用 **Vertical Glow + 1px Border**
- ❌ **拒绝**: R3F 在 Dashboard 区域 (仅限 Hero)

---

## 4. UI/UX 风格指南

### 4.1 设计理念: "Terminal Minimalism" (终端极简)

融合 **Linear 风格** + **Computational Beauty** + **Solana 品牌色**，打造"活体数据"界面。

> **核心隐喻**: 不是"精致的仪表盘"，而是"透明的生物实验室" —— 你能透过玻璃看到 Agent 的神经脉冲（资金流）在实时跳动。

- ✨ **Glass & Depth**: 多层级的磨砂玻璃 (Backdrop Blur) 与精细的 1px 边框
- 🌑 **Deep Texture**: 背景叠加动态噪点纹理，避免死板的纯色
- 🟣 **Subtle Glow**: 交互时触发细腻的光晕 + 磁吸效果
- 💻 **Terminal Realism**: 终端风格日志，JetBrains Mono 主导数据展示
- 🔮 **Living Data**: Hero 区粒子网络，展示协议"呼吸感"

### 4.2 色彩系统 (Refined)

```css
/* 品牌基调 - 降低饱和度，提升高级感 */
--color-primary: #9945FF;        /* Solana Purple */
--color-primary-dim: rgba(153, 69, 255, 0.1);

--color-success: #14F195;        /* Solana Green */
--color-success-dim: rgba(20, 241, 149, 0.1);

/* 背景层次 - 质感核心 */
--bg-page: #09090B;              /* Zinc 950 + Noise */
--bg-card: rgba(255, 255, 255, 0.03); /* 极低透明度白 */
--bg-card-hover: rgba(255, 255, 255, 0.06);

/* 边框系统 - 细腻的分隔 */
--border-subtle: rgba(255, 255, 255, 0.08);
--border-highlight: rgba(255, 255, 255, 0.15);

/* 文字层次 */
--text-primary: #FAFAFA;         /* Zinc 50 */
--text-secondary: #A1A1AA;       /* Zinc 400 */
--text-tertiary: #52525B;        /* Zinc 600 */
```

### 4.3 字体规范 (Terminal-First)

- **UI**: `Inter Tight` (导航、按钮、描述文字), Weight: 400/500
- **Data/Terminal**: `JetBrains Mono` (扩大使用范围), Weight: 400
  - Hash / Amount / Timestamp
  - 实时日志全部内容
  - KPI 数字
  - 大标题 (可选，增强科技感)
- **大小**: 使用更小的字号配合更大的留白 (Whitespace)，营造呼吸感
- **终端高亮**: 关键字段着色 (ORDER_ID 青色, SOL_AMOUNT 绿色, ERROR 红色)

### 4.4 质感特效 (Utility Classes)

```css
/* 玻璃拟态卡片 */
.glass-card {
  background: var(--bg-card);
  backdrop-filter: blur(12px);
  border: 1px solid var(--border-subtle);
  box-shadow: 0 4px 24px -1px rgba(0, 0, 0, 0.2);
}

/* 顶部光效 - 模拟顶光照射 */
.top-highlight {
  box-shadow: inset 0 1px 0 0 rgba(255, 255, 255, 0.08);
}

/* 渐变文字 */
.text-gradient {
  background: linear-gradient(to right, #ffffff, #a1a1aa);
  -webkit-background-clip: text;
  color: transparent;
}
```

---

## 5. 页面结构与布局

### 5.1 布局策略: "Bento Grid" (便当盒布局)

放弃传统的三栏布局，采用 **Grid 布局**，将模块视为独立的"功能块"，保持整洁对齐。

```
/dashboard
├── [Header Area] (Logo + Nav + Wallet + Status)
├── [Main Grid]
│   ├── [Hero Stats] (KPI Numbers)
│   ├── [Live Feed] (左侧: 实时交易流)
│   └── [Visuals]   (右侧: Agent Flow Graph + 趋势图)
```

### 5.2 响应式设计

- **Desktop First**: 优先保证大屏下的信息密度与留白平衡。
- **Mobile**:不仅是堆叠，隐藏次要数据，保留核心状态流。

---

## 6. 核心界面设计 (Mockup)

### 6.1 Dashboard (核心视窗)

```
┌──────────────────────────────────────────────────────────────────┐
│  ❖ Exo Protocol      Dashboard   Skills   Docs          [Connect]│
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  [ KPI Cards: Glass Panel ]                                      │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────────┐  │
│  │ Total Volume     │ │ Active Skills    │ │ Avg Latency      │  │
│  │ 1,234.56 SOL     │ │ 8                │ │ 845ms            │  │
│  │ ↗ +12%           │ │                  │ │ ⚡ Fast          │  │
│  └──────────────────┘ └──────────────────┘ └──────────────────┘  │
│                                                                  │
│  [ Split View: Grid Gap 24px ]                                   │
│  ┌──────────────────────────────┐  ┌──────────────────────────┐  │
│  │  Live Transactions           │  │  Fund Distribution       │  │
│  │                              │  │                          │  │
│  │  12:34:56  Order #8291...3f  │  │   [Clean Sankey Chart]   │  │
│  │  [Status: Executed]          │  │                          │  │
│  │  ───                         │  │   Users ──┐              │  │
│  │  12:34:50  Order #8291...2a  │  │           ├─ Protocol    │  │
│  │  [Status: Settled +1.2s]     │  │   Agents ─┘  (Fees)      │  │
│  │  ───                         │  │                          │  │
│  │  12:34:42  New Skill Publish │  │   Creator ── Agent       │  │
│  │  "Twitter-Sentiment-V2"      │  │   (Rewards)              │  │
│  │                              │  │                          │  │
│  │  [Blur Fade Overlay]         │  │                          │  │
│  └──────────────────────────────┘  └──────────────────────────┘  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

**设计细节**:
- **KPI Cards**: 无背景或极淡背景，JetBrains Mono 大数字，Number Ticker 滚动动画
- **Terminal Feed**: 终端风格替代瀑布流
  - 强制 `JetBrains Mono` 字体
  - 关键字段高亮: `ORDER_ID` (cyan), `SOL` (green), `LATENCY >1s` (red)
  - 新数据出现时有极快的"扫描线"扫过效果
  - 底部淡出 (Mask Image fade)
- **Agent Flow**: React Flow 节点图替代静态 Sankey
  - 节点: User / Protocol / Executor / Creator
  - 边: 动画粒子流，展示资金流向
  - 实时: 交易完成时粒子加速冲入目标节点
  - ⚠️ **UX 约束**: 锁定视图 (禁止 Zoom/Pan)，作为"动态图表"而非"编辑器"
    ```tsx
    // React Flow 配置
    <ReactFlow
      zoomOnScroll={false}
      zoomOnPinch={false}
      panOnScroll={false}
      panOnDrag={false}
      nodesDraggable={false}
      nodesConnectable={false}
      elementsSelectable={false}
      fitView
    />
    ```

### 6.2 首页 (Landing) - "The Pulse" 活体首页

```
┌──────────────────────────────────────────────────────────────────┐
│  [Background: R3F Particle Network + Zinc-950]                   │
│                                                                  │
│              ·  ·    ·                                           │
│           ·    ╲  ╱    ·     ← 粒子网络 (代表 Agent 生态)        │
│         ·  ──── ◉ ────  ·       交易时紫色脉冲闪烁               │
│           ·    ╱  ╲    ·                                         │
│              ·  ·    ·                                           │
│                                                                  │
│      The Agent Economy,          ← 打字机效果逐字显现            │
│      Enforced by Code.                                           │
│      ──────────────────                                          │
│                                                                  │
│      [ TPS: 2,847 ]  [ Volume: 1,234 SOL ]  ← 实时数据滚动       │
│                                                                  │
│      [ Button: Enter Dashboard (Magnetic + Glow) ]               │
│                                                                  │
│   [ Feature Icons: Minimal Strokes ]                             │
│   ◇ Token-2022 Hooks    ◇ Compressed Identity    ◇ Atomic Split  │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

**Hero 交互细节**:
- **粒子网络**: R3F Drei Points，低多边形，深色背景下微光
- **脉冲效果**: WebSocket 监听链上事件，每笔交易触发一道紫色光线
- **打字机**: 标题逐字显现，模拟终端输出
- **磁吸按钮**: 鼠标靠近时按钮轻微被"吸"过去 (Framer Motion)

---

## 7. 组件库定制 (shadcn/ui overrides)

### 7.1 Card (`ui/card`)

- **去除**: 默认的白色背景和深色阴影。
- **添加**: `bg-zinc-900/50 backdrop-blur-md border-zinc-800/50`.

### 7.2 Button (`ui/button`)

- **Primary**: 白色文字，黑色背景 (或极深紫)，微发光 `shadow-[0_0_20px_rgba(153,69,255,0.3)]`。
- **Secondary**: 全透明，仅保留 1px 边框和白色文字。

### 7.3 Badge (`ui/badge`)

- 使用 `outline` 变体为主。
- 背景色透明度极低 (e.g., `bg-green-500/10 text-green-500 border-green-500/20`)。
- 字体使用 Mono 等宽字体。

---

## 8. 演示策略 (Living Data Edition)

### 8.1 视觉叙事流

1. **活体开场 (Hero)**: 粒子网络在黑暗中呼吸，打字机效果输出标题，实时 TPS 滚动
2. **终端律动 (Dashboard)**: 日志以终端扫描线方式滑入，关键字段高亮闪烁
3. **资金具象 (Agent Flow)**: React Flow 节点图，粒子从 User 流向 Executor，交易完成时加速冲入

### 8.2 质感增强点

- **Cursor Spotlight**: 大光圈、极低透明度，模拟手电筒照亮精密仪器
- **Number Ticker**: 数字变化时滚动动画 (Tabular nums)
- **Magnetic Buttons**: 鼠标靠近时按钮被轻微"吸"过去
- **Scan Line**: 新数据出现时的水平扫描线效果

### 8.3 声音设计 (P3-C 可选)

- 连接钱包: 极轻微的高科技确认音
- 交易完成: 短促的脉冲音效
- 音量: 默认关闭，用户可开启

---

## 9. 开发优先级 (Phased Delivery)

### Phase 3-A: 基础架构 (Foundation) - 必须
1. Next.js 15 项目初始化 + TanStack Query + Zustand
2. Tailwind 颜色系统 + CSS Variables + CVA
3. 封装 `GlassCard` + `SpotlightBackground` + `MagneticButton`
4. Dashboard Bento Grid 布局框架

### Phase 3-B: 数据可视 (Visualization) - 必须
1. `TerminalFeed` 终端风格日志组件 (扫描线 + 高亮)
2. `AgentFlowGraph` React Flow 节点图 (粒子动画)
3. KPI Cards + Number Ticker
4. 对接 P3-MOCK 数据

### Phase 3-C: Hero 与打磨 (Polish) - 高优先
1. R3F 粒子网络 Hero (轻量实现)
2. 打字机效果标题
3. 全局 Cursor Spotlight
4. 字体加载优化

### Phase 3-D: 增强 (Enhancement) - 可选
1. 声音设计 (默认关闭)
2. WebSocket 实时数据 (依赖 Helius API)
3. 移动端适配优化

---

## 10. 设计决策记录 (ADR-011)

### 已确认决策

| 决策项 | 结果 | 说明 |
|-------|------|------|
| **风格** | Terminal Minimalism | 融合 Linear + Computational Beauty |
| **背景** | Zinc-950 + 动态噪点 | 非纯黑，有呼吸感 |
| **字体** | JetBrains Mono 主导 | 扩大 Mono 使用范围，强化终端感 |
| **Hero** | R3F 粒子网络 | 轻量实现，展示"活体"概念 |
| **数据流** | React Flow 替代 Sankey | 动态节点图 + 粒子动画 |
| **交互** | 磁吸按钮 + 扫描线 | 增强科技感 |
| **声音** | P3-D 可选 | 默认关闭，不阻塞主流程 |

### 风险缓解

| 风险 | 缓解措施 |
|------|----------|
| R3F 性能 | 仅 Hero 使用，Dashboard 纯 2D |
| 开发时间 | 分 4 阶段，P3-D 可选 |
| Bundle Size | R3F 按需 import，tree-shaking |

---

*文档生成: CSA Protocol v3.3.0 | ADR-011 APPROVED*
