# P10-L01: 统一 UI (添加 Header，修复 Mock 数据时间)

## Meta
- **Type**: `Simple / UI`
- **Risk Level**: 🟢 Low
- **depends_on**: 无 (独立任务)
- **Priority**: P1 (锦上添花)

## 核心目标

**统一 Dashboard 页面布局** - 添加 Header 组件保持全站导航一致性；动态化 Mock 数据时间戳让数据看起来"活"。

## Input Files
- `exo-frontend/app/dashboard/page.tsx` (L1-L30) - 添加 Header 导入和 padding
- `exo-frontend/lib/api.ts` (L11-L17) - fetchOrders 函数修改

## External Dependencies
| 资源 | 类型 | 状态 |
|------|------|------|
| Header 组件 | 本地文件 | ✅ 已存在 `components/layout/header.tsx` |

## Action Steps

### Step 1: Dashboard 添加 Header (`app/dashboard/page.tsx`)

**1.1 添加 import**:
```tsx
import { Header } from "@/components/layout/header";
```

**1.2 修改 return 结构**:
```tsx
return (
    <>
        <Header />
        <main className="container mx-auto p-4 md:p-8 pt-20">
            {/* pt-20 = pt-16 (header高度) + pt-4 (额外间距) */}
            {/* ... 现有内容 */}
        </main>
    </>
);
```

### Step 2: Mock 数据时间动态化 (`lib/api.ts`)

修改 `fetchOrders` 函数，将返回的订单时间戳替换为动态值：

```typescript
export async function fetchOrders(): Promise<Order[]> {
    const response = await fetch(`${BASE_URL}/mock/mock_orders.json`);
    if (!response.ok) {
        throw new Error(`Failed to fetch orders: ${response.status}`);
    }
    const orders: Order[] = await response.json();
    
    // 动态化时间戳: 当前时间 - 随机分钟数 (0-60分钟内)
    const now = Date.now();
    return orders.map((order, index) => ({
        ...order,
        created_at: new Date(now - (index * 3 + Math.random() * 5) * 60 * 1000).toISOString(),
    }));
}
```

**时间分布逻辑**:
- 第1条订单: 0-5 分钟前
- 第2条订单: 3-8 分钟前
- 第N条订单: (N*3) ~ (N*3+5) 分钟前
- 确保订单按时间倒序排列 (最新在前)

## Constraints
- Header 固定定位 (`fixed top-0`)，需要 `pt-20` 避免内容遮挡
- 时间戳使用 ISO 格式 (`toISOString()`)
- 不修改 mock JSON 文件本身

## Verification
- **Visual**: Dashboard 顶部显示 Header，可点击导航到 Skills/Docs
- **Visual**: Live Transactions 中的时间显示为 "X minutes ago" 而非固定日期
- **Evidence**: 刷新页面，时间戳随之更新

## Rollback
- `git checkout -- exo-frontend/app/dashboard/page.tsx exo-frontend/lib/api.ts`

---

*Generated by CSA Protocol v4.2*
*Created: 2025-12-19 01:41 UTC+8*
